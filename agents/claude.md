# Claude/Anthropic Binding

Use the skill folder at:

```text
skillpacks/anthropic-claude/preserve-project-conversations/
```

The `SKILL.md` in that folder instructs the agent to preserve the five-layer memory contract and generate lecture/development/library/publishing views from those layers.

Recommended user prompt:

```text
Use $preserve-project-conversations to preserve this project chat as five-layer memory, then make lecture notes and a searchable library entry.
```
